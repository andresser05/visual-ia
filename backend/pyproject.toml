[project]
name = "vaccine-recommender-backend"
version = "0.1.0"
description = "Backend for vaccine recommendation ML model"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "scikit-learn>=1.5.0",
    "pandas>=2.2.0",
    "joblib>=1.4.0"
]
